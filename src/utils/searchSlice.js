import { createSlice } from "@reduxjs/toolkit";

const searchSlice = createSlice({
    name:"search",
    initialState:{

    },
    reducers:{
        store: (state,action) =>{
            
        },
        filter: (state,action) =>{

        }
    }
})